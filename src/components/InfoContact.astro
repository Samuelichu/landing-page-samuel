---
interface Props {
    titulo: string;
    descripcion: string;
}

const { titulo, descripcion } = Astro.props;
---

<div class="flex flex-col">
  <p class="text-secondary opacity-50 font-medium text-lg">{titulo}</p>
  <p class="text-secondary font-medium text-xl">{descripcion}</p>
</div>
